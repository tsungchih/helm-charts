apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debezium-kafka-connect.fullname" . }}-test-connection"
  labels:
    {{- include "debezium-kafka-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debezium-kafka-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
