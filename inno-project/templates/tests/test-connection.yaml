apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inno-project.fullname" . }}-test-connection"
  labels:
{{ include "inno-project.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "inno-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
